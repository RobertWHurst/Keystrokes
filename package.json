{
  "name": "@rwh/keystrokes-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r --sort build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@parcel/packager-ts": "2.9.3",
    "@parcel/transformer-typescript-types": "2.9.3",
    "typescript": "^5.1.6"
  }
}
